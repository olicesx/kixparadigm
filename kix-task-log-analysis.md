# KIX 范式执行日志分析报告（实测数据）

> 分析对象：`~/.dsh/sessions` 下全部 85 个会话（2026-08-14 ~ 08-15，kixparadigm preset）
> 方法：解码 DSH `session.jsonl.zstd`（多帧 zstd + JSONL），统计每会话的思考 token（reasoning-chunks）、回答 token（text-chunks）、步骤、工具调用与子代理血缘（parentSession）。
> 口径说明：① 日志中可精确度量的是**完成侧 token（思考 + 回答）**；输入侧 token（每次请求重发的上下文）**未写入日志**，实际账单通常远大于完成侧，下文涉及输入处均标注为估算；② 思考 token 数为流式 chunk 行中 token 字符串计数，为近似值。

---

## 1. 总体画像

| 维度 | 数值 |
|---|---|
| 会话总数 | 85（主会话 53 + 子代理 32） |
| 主会话步骤 | 4,267 步；工具调用 4,947 次；用户消息 390 条 |
| 主会话思考 token | **2.45M**（回答仅 214k，**思考占完成侧 92%**） |
| 子代理思考 token | 428k（回答 85k，思考占 83%） |
| 全部完成侧 token | **≈ 3.18M，其中思考占 90.6%** |
| 子代理 spawn | 17/53 主会话产生，共 38 次（subagent 19 / subagent_zhipu 14 / workflow 4 / subagent_vision 1） |
| 重度会话 | 17 个主会话思考 >50k，合计 1.91M，**占主会话思考的 78%** |
| 项目分布 | kix-bundle 40 会话 / R 1.26M；私有项目 B 7 / R 557k；私有项目 C 3 / R 547k；公开开源项目 1 / R 77k |
| 子代理模型 | deepseek-v4-flash ×15、glm-5.2 ×11、glm-4.6v 系 ×5、deepseek-v4-pro ×1 |

**一句话：两天 3.18M 完成 token 里 90% 是「想」，且集中在 17 个重度会话。**

---

## 2. 发现 ①：max 思考强度全面透支（根因确认）

**证据（最大会话：私有项目 C 的 PR 审查，08-15 05:23）：**
- 197 步，思考 **241.8k** vs 回答 **4.7k → 98.1% 的完成 token 是思考**。
- 机械步骤也在全功率思考：
  - step 39（`grep` + `pwsh`）：思考 **17,433** token
  - step 40（`pwsh`）：思考 **17,136** token
  - step 33（`web_search` + `grep`）：思考 15,917 token
  - step 37 / 38（`grep`/`read`）：思考 11,020 / 10,401 token
- 即：**一次 grep 前的「想」就花掉 1-1.7 万 token**——这正是 `settings.yaml` 全局 `reasoningEffort: max` 的直接后果。

子代理同样中招：review-of-review 子代理（deepseek-v4-flash，子代理会话 F）单会话思考 **105.8k**、84 步——子代理继承父路由 max，验证任务的「验证者」也全功率想。

---

## 3. 发现 ②：轮询等待 = 双重浪费（新增发现，此前方案未覆盖）

**证据（同一 PR 会话）：**
- 91 个步骤是 `list_agents` / `job_output` / `job_list` 轮询调用，**纯轮询就烧掉 40.9k 思考 token**（占该会话思考的 17%）。
- 最夸张的单步：step 52 只调了一次 `list_agents`，思考 **6,466** token。
- 双重浪费：① 每轮轮询都是一次完整的 max 思考模型回合；② 每次轮询都要**重发整段会话上下文**（该会话到第 146 步时上下文已含 ~240k 历史 token，含全部思考回放），输入侧成本随步骤数超线性增长。

> 估算：197 步 × 平均上下文 ~50k 输入 token ≈ **该会话输入侧接近 10M token 量级**（DeepSeek 自动上下文缓存可部分折扣，但轮询打散了前缀复用模式）。轮询步骤越多，输入侧浪费越大。

**根因**：主 agent 把子代理当「同步等待」——spawn 后不继续做别的，而是循环 `list_agents` 空转。DSH 的机制是「后台子代理完成后有通知」，不需要轮询；且 spawn 时 `run_in_background: false` 的前台等待即可（部分调用已如此）。

---

## 4. 发现 ③：跨会话零记忆，同一任务重复支付

**证据：**
- **私有项目 C 的同一 PR 被完整审查了 3 次**（08-14 10:28 / 08-15 05:22 / 08-15 05:23），每次结构完全相同（`subagent_zhipu` 安全审查 + `subagent` QA 行为边界），三次合计约 **547k 思考 token**；且每次仍各 spawn 2 个子代理（glm-5.2 17-20k + deepseek 19-106k）。
- **私有项目 B 的「UDP 4-socket 改造风险评估」+「pool 摇摆分析」在 3 个会话里用逐字相同的 prompt 重跑了 3 次**（08-14 16:24 / 22:18 ×2）。
- 同一 PR/同一技术议题的结论没有落盘复用——每次会话从零开始「三通道验证」。

**含义**：KIX 的 progress.md / memories 机制没有覆盖「外部验证结论」的复用。三通道观察子代理是最贵的 token 来源之一，重复支付最不值。

---

## 5. 发现 ④：11 子代理 fan-out 的编排成本

**证据（08-14 18:43 kix-bundle 会话）：**
- 一次「梳理 DSH 全部包」任务 spawn **11 个子代理**（6× `subagent` + 5× `subagent_zhipu`，每个负责一组包 + 1 个插件审查）。
- 子代理个体成本不高（2-16k 思考 each），但：父线程编排 87 步 + 11 个子代理会话 + 期间的轮询/汇总，父侧思考 58.4k，总账 ~120k+。
- 对照 [More Agents Is All You Need](https://ar5iv.labs.arxiv.org/html/2402.05120)：此类**平行检索型 fan-out 收益在 4-8 个后饱和**；11 个里相当一部分（尤其同质 `subagent`×6）贡献递减。

**正面案例对照**：三通道验证通常 spawn 2 个（1 异质 GLM + 1 同源），结构本身符合论文建议的「异质 2-3 个封顶」——问题在数量失控场景（11）和轮询等待。

---

## 6. 发现 ⑤：workflow 双跑重试

**证据（08-14 16:06 会话）：**
- `workflow vision-read-png` 执行后，同一会话立刻又跑 `vision-read-png-2`（重试版）——**同一读图任务两次完整 workflow 开销**（每次含 1 个视觉子代理 + 编排）。
- 同时该会话还先 spawn 了一个 `subagent` 读图（描述「读取图片文字」）——**同一张图在同一会话里被三种方式读了三次**（subagent → workflow → workflow 重试），说明当时视觉链路不熟导致的试错；但试错成本全部落在 max 思考的主模型上（该会话 R 79.9k / 21 步）。

---

## 7. 结论：日志证据 ↔ 优化方案映射

上一份报告（`kix-subagent-token-optimization.md`）的每条建议，现在都有实测数据支撑：

| 方案 | 日志证据（本次实测） | 预期收益 |
|---|---|---|
| P0.1 全局 effort 降档（max→medium） | 思考占完成侧 90.6%；单次 grep 前想 1.7 万 token；98% 思考的会话 | 完成侧思考 token 预计 -50~70%（s1/e1 论文锚点） |
| P0.2 角色分档工具行（lite/thinker） | 机械步骤（grep/read/pwsh）与高难步骤同功率；子代理 15/32 是继承 max 的 deepseek | 机械观察用 glm-4.5-air 后单步成本降一个数量级 |
| P0.3 per-role maxTokens 预算帽 | 单步思考最高 1.7 万、单次轮询 6.5k，无上限 | 跑飞步被硬性截断 |
| P0.4 观察者数量门控 | 17 个重度会话占 78% 思考；11 子代理 fan-out；同构子代理重复 | fan-out 收敛到 2-3 个异质观察者 |
| **新增 P0.5 禁止轮询空转** | 91 步轮询 / 41k 思考 / 上下文重复重发 | 每重度会话省 ~15-20% 完成侧 + 大量输入侧 |
| P1.3 验证结论 memoization | 同一 PR ×3 次 = 547k 重复思考；同议题 ×3 次逐字重复 | 同任务二次执行直接复用结论 |
| P1.2 输出 schema 化 | 子代理返回长叙述（如子代理 F T 1.8k+回放） | 减少回流与父侧重读 |
| P2.2 上下文缓存 | 轮询/多子代理场景上下文反复重发 | 输入侧命中价 ~1/10 |

**新增的优先级建议（日志驱动）：**
1. **P0.5（最高优先，纯行为规则）**：persona §CEO 编排加一条「子代理 spawn 后不得用 list_agents 轮询等待」——要么 `run_in_background: false` 前台等结果，要么后台 spawn 后先做其他独立工作，等系统通知。轮询是本次日志里最纯的浪费（不产出任何信息，只重发上下文）。
2. **P0.6 跨会话结论复用**：把「外部验证结论」（PR 审查结论、技术风险评估）按 `(repo, 议题, 日期)` 写入项目级 `progress.md`/`memories`，spawn 前先查。
3. **P1.5 同会话内去重**：同一会话内同任务（如读同一张图）先查已执行记录，禁止三种方式重复执行。

---

## 8. 度量建议（把账算清）

当前日志**不记录 usage/计费数据**（每步只有 provider/model，无 prompt/completion 计数），本次分析只能精确度量完成侧。建议：
1. 在 session telemetry / 存储层增加 per-step usage 记录（prompt_tokens / completion_tokens / cached_tokens），成本才能完全归因到「思考、轮询、重复」三类。
2. 把 per-role token 趋势并入现有 `verification-fidelity-check.ps1` 的跨 Sprint 累积机制，用数据驱动 P0/P1 各项的开关。

---

## 附：本报告关键会话索引

| 会话 | 项目 | 规模 | 特征 |
|---|---|---|---|
| 会话 A（08-15 05:23） | 私有项目 C（PR 审查） | R 240.4k / 197 步 | 98% 思考、91 轮询步、2 子代理 |
| 会话 B（08-14 10:28） | 私有项目 C（PR 审查） | R 154.3k / 250 步 | 同 PR 第一次审查、76 次 list_agents |
| 会话 C（08-14 22:18） | 私有项目 B | R 185.8k / 513 步 | 风险评估 ×3 中的一次 |
| 会话 D（08-14 18:43） | kix-bundle | R 58.4k / 11 子代理 | fan-out 11 编排 |
| 会话 E（08-14 16:06） | kix-bundle | R 79.9k / 21 步 | 读图三连：subagent + workflow ×2 |
| 子代理 F | 私有项目 C | R 105.8k / 84 步 | review-of-review 子代理继承 max |

> 注：会话以字母代号标注；具体会话 ID、本机路径与 prompt 内容属个人信息，不入库。
