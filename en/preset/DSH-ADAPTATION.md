# DSH Adaptation Layer — kixParadigm × DeepSeek Harness (EN edition, condensed)

> Authoritative mechanism mapping for this English preset. When the Chinese kix originals (skills/prompts/memories, pending translation) conflict with this file or with `agent.cordis.yml`, **this preset's `agent.cordis.yml` persona wins**; mechanism details below are the authority for tool usage.

## §1 Tool-name mapping (Copilot → DSH)

| kix docs (Copilot) | DSH equivalent | Notes |
|---|---|---|
| `runSubagent agentName: "kixpower-xxx"` | `subagent` (no agentName) + role body of `agents/kixpower-xxx.agent.md` injected into the prompt + [CONTEXT]/[TASK]/[CONSTRAINTS] block | `subagent_fork` when the child must inherit this session's context |
| subagent tiers (v5.8 cost discipline) | `subagent_lite` (mechanical: glm-4.7 slim composition, 8K cap) / `subagent` (ordinary, high, 64K cap) / `subagent_thinker` (kix-route:thinker → deepseek, 128K cap) / `subagent_cross` (kix-route auto inverted-vendor, 64K cap) / `subagent_vision` (auto-routed to the first image-capable model) | effort normalized by `plugins/kix-cost.js` / `plugins/kix-route.js`; cross/vision/thinker routes and the lite fallback all resolve against live catalogs |
| `vscode_askQuestions` | `ask_user_question` | user-owned choices / material ambiguity |
| `read_file` / `grep_search` | `read` / `grep` | same semantics |
| `replace_string_in_file` | `edit` | literal old_string/new_string |
| `run_in_terminal` | `pwsh` (Windows) / `bash` (POSIX) | default `pwsh` 7.x |
| PreToolUse hooks | `plugins/kix-guards.js` (tools/pre-execute; v9 confirmation-class gates are soft constraints) + host sandbox stack | kixpower `hooks/*.ps1` kept as reference + manual checks |
| slash commands `/kixpower-*` | native commands registered by `plugins/kix-commands.js` | type `/` in the UI; zero-token trigger; handler injects `prompts/*.prompt.md` body as a user message |

## §2 Mechanical guards (resident)

- `plugins/kix-guards.js` (210 assertions, v9) auto-mounts a `tools/pre-execute` listener (scope-filtered to this preset) that intercepts: destructive SQL (DROP/TRUNCATE/DELETE/UPDATE without WHERE, statement-level, comment-stripped; terminal DB clients require command-position detection + SQL payload analysis, so `echo "psql DROP"` no longer trips), dangerous git (force push incl. `-f`/`+refs`/`--mirror`, main-branch pushes, commit-budget via reflog subject — commit-type entries only, stale-sprint fallback to max-numbered dir, plan.md max_commits fallback chain, cold-start warn; v7), control-plane file writes (v8: write intent only — `grep/cat/ls ~/.dsh` are allowed; write commands and redirections targeting the control plane are denied), unknown execution tools, MCP GitHub remote writes without a feature branch (tool prefix configurable via `githubToolPrefix`). v9: publish/comment/ordinary-push confirmation gates are soft constraints — explicit user instruction is the decision and no per-operation question is asked; hard deny remains for irreversible destruction only.
- Human-decision soft constraint (v9, v1.2.11): publish/comment/merge/destructive actions are off by default; an explicit user instruction ("comment on the PR") is the decision and executes directly. `ask_user_question` is reserved for genuinely missing decision information. Hard deny remains only for mechanical irreversible destruction.
- The harness's sandbox stack is the resident mechanical layer on top; the approval policy for `danger-full-access` is `never` (fully automatic, no approval dialogs).

## §2.1 Discipline & orchestration gates (pluginized, 2026-08-16)

- **`plugins/kix-discipline.js`** (68 assertions): the requirement grill & verification gate moved from prompt preaching to enforced mechanism. `tools/pre-execute`: implementation edits (`edit`/`write`) check for a requirement contract (spec) on record — no spec + first implementation edit → `remind` (default: allow + one-shot reminder injected via post-execute) / `ask` (in-chat question) / `block` (deny); test files always pass. `kix_discipline_spec` tool records the triple-check contract (goal/xy/assumptions/path/acceptance, matching kix ①XY ②premise ③path) into workspace `kix-discipline/spec.md` (visible across sessions). `agent/turn-stopping`: a turn ending with implementation edits but no test run gets the delivery-three-questions green reminder (remindOnce). `/kix-discipline status|report|on|off`. Intensity defaults to `remind` (fewer constraints; literal low-risk reversible tasks may ignore the reminder — matches kix signal-based triggering); `ask`/`block` need explicit config on the `agent.cordis.yml` row. Boundary: agent-scope mounted, does not cover subagent sessions (same as kix-guards).
- **`plugins/kix-orchestration.js`** (93 assertions): orchestration handoff gate — a bounded port of Copilot's `validate-handoff.ps1` core. `tools/pre-execute` on `subagent*` dispatch carrying `current_sprint` validates: workspace `docs/.kixpower-current-sprint` marker exists & matches, `docs/sprint-N/{plan,progress}.md` exist (planning done before Dev/QA), `progress.md` has no blocker (`status: blocked` / `blocked_tasks>0` / `❌ Blocked`), QA handoff requires `completed_tasks == total_tasks`; v3 adds the `producer_closeout` evidence chain (spec acceptance on record / completed==total / test files unchanged since `l2_verified_sha`). **v2 return-side check** on `subagent/end`: a QA child returning an **explicit positive** completion claim while `progress.md` is out of sync gets a one-shot steer reminder; v8 excludes negated statements (`not done`, `undone`, `not passed`, `passed nothing`) before matching. **v4 (2026-08-17, WSL2-field-tested)**: sleeping to wait for background subagents (`sleep N` / `Start-Sleep` in any tool's `command` + a description mentioning subagent) gets a one-shot reminder to end the turn instead — settlement/report delivery wakes a new turn unconditionally (see §3.1). Default `remind`; `ask`/`block` configurable. `/kix-orchestration status|on|off`. **Not ported** (bound to Copilot's runSubagent+agentName dispatch format; over-porting = liability, see PLUGINIZATION-ROADMAP.md): worktree registration / plan_snapshot_sha / l2_gate_manifest_sha256 / stash baseline / reverify marker deep checks. **v11 (2026-08-17, P5)**: writing `docs/sprint-N/plan.md` via `write` runs `checkPlanContract` — budget-source fields (`task_sizing.derived_commit_budget` / `blast_radius.max_commits`; absent → budget silently falls to cold-start 3, the sprint-9 accident shape) plus task-list existence (`-`/`*` bullets); `edit` is not validated (no full new content — 0-false-positive discipline); gated by the session `enabled` switch (`/kix-orchestration off` disables it, same as the handoff/sleep checks); default `remind`, dedicated slot (does not burn handoff/sleep slots).
- Copilot hooks status: of the 9 `hooks/*.ps1`, blast-radius → kix-guards; validate-handoff core → kix-orchestration; role-boundary hooks (block-source-edit / block-dev-authority / block-source-edit-qa) stay as prompt hard constraints (DSH subagents carry no role marker); validate-qa-signoff / qa-freshness / cleanup-qa-session / auto-update-progress have no DSH equivalent flow — not ported. Full audit: `PLUGINIZATION-ROADMAP.md` (EN condensed; CN full edition in the CN preset, incl. migration-completeness audit §6.1).
- **`plugins/kix-focus.js`** (83 assertions): minimal + progressive disclosure + PTC synergy (P4, 2026-08-16). Phase 1 `tools.restrict({ allow })` trims the per-turn tool surface from ~85 (~108KB schema, est. ~30.8K tokens) to the resident core — **allow lists only GLOBAL tools** (`RESTRICT_ALLOW` ~11: edit/write/pwsh/read/grep/glob/ask_user_question/todo_write/skill/web_search); the subagent×5 and kix_capability_* rows are **scope-registered and auto-visible — not listed in allow** (DSH restrict contract fails on scope-local names). MCP (GitHub/Playwright/Context7/Semgrep), workflow/goal/ralph/job_*/cordis_* stay on demand; `tools/change` retry; measured **-81.6%** (`scripts/quantify-focus.cjs`). Phase 2 `kix_capability_search` (GLOBAL view `schemas(undefined)` lists trimmed tools; group metadata without full schemas) + `kix_capability_call` (`get(name, undefined)` global existence check; proxy via `ctx.tools.execute` — full pre-execute→guards→execute→post-execute, gates still fire; **propagates `rootCallId`** for nested execution-tree attribution; agent-carrying calls are not model-direct so no UNKNOWN_TOOL). **Sensing design (revised 2026-08-16): NO pre-execute deny** — restrict already hides trimmed tools from the model (direct calls = UNKNOWN_TOOL before pre-execute) and capability_call's nested sub-calls must pass; guidance rides the call return + persona trigger phrases. Phase 3 keeps `tool-presentation mode: both`; kix red line "verification/observation stay native (evidence replayable)" unchanged; capability_call is also callable from run_code SDK sub-dispatches (which pass through gates). Config: `enableRestrict: false` disables trimming (search/call only); `extraResidentTools` appends. Guards interaction: capability_call/search are in `KNOWN_SAFE_TOOLS` (defensive); every proxied sub-call still passes kix-guards. **2026-08-17 (user decision A+B)**: job_* (job_output/job_list/job_kill) became resident — background jobs always available, fixing the "background jobs unavailable: no job controller serves this agent" error that occurred while `tool-jobs` was disabled; unmounted subagent tiers and goal **auto-activate on first use** — `kix_capability_call { tool: subagent_qa, … }` mounts the package via `ctx.plugin` and continues executing (mechanism-level activation; the model never has to remember `kix_tool_activate`, which remains as explicit pre-activation, `kix_tool_deactivate` unloads).
**Write-time consistency guard (`kix-consistency`, added 2026-08-17, P5)**: the CI script only validates at test time; edits to preset files were not intercepted in real time. This plugin turns the "single source of truth" convention from self-discipline into mechanism (see `PLUGINIZATION-ROADMAP.md` P5):
- `scripts/check-dsh-consistency.cjs` refactored into a pure-function core `plugins/consistency-lib.cjs` — **CI scripts and the plugin share one implementation** (root-parameterized, returns `{failures, notes}`, no console side effects), preventing CI-vs-runtime dual-source drift
- `tools/pre-execute`: writes under `dsh/preset/`, `en/preset/`, README*, package.json*, vision-bridge/bridge run the **relevant sub-checks** by path (persona budget / plugin pair sync / memories count / README phrases / version pair / single-file syntax); failure → `remind` (default) / `ask` / `block` (configurable). Plugin-source matching covers `.js`/`.cjs` — the shared lib itself is guarded too (same scope as the CI's dynamic plugin list)
- Trigger surface: only "source-repo fingerprint" workspaces (dsh/preset + en/preset + scripts entry all present) — other workspaces pass through with zero overhead; remindOnce per session per category, pending reminders keyed by callId (concurrent multi-category writes do not drop each other's reminders)
- CI relationship: the plugin-name list is dynamic (`pluginNames()` scans the directory); new plugins join CI checks automatically, no hardcoded list to maintain

## §3 Delegation, cost tiers & vision

- **Cross-vendor observer**: three-channel observation / highest-confidence claims / platform-library semantics assertions → `subagent_cross` (GLM-5.3, cross-vendor contradiction). Ordinary dispatch → `subagent` (inherits the main model). `workflow`'s `agent(prompt, {provider, model})` overrides per call. Since v5.9 the cross/vision/thinker rows pin only a `kix-route:<tier>` sentinel model name; `plugins/kix-route.js` resolves it in the child's first agent/request waterfall against the live llm catalog — cross = vendor inverted from the parent, vision = first model declaring image input, thinker = deepseek preferred. **Boundary semantics (v5.9.1: fail when the role's core capability is missing, degrade when the role still stands)**: cross in a single-vendor deployment (no foreign provider registered) and vision with no image-declaring model anywhere FAIL AT START — the error lists registered providers plus what to do instead (plain subagent same-vendor review with an explicit caveat / configure the provider) and reaches the parent via the failed run; never silently degrade to same-vendor (false independence is worse than failure). Thinker without deepseek degrades to the environment default route (the big-budget deep-thinking role still stands) with a one-time warning; resolution failures are not cached (a provider registered mid-session takes effect on the next request); a missing plugin fails LOUD on the sentinel (UNKNOWN_MODEL). The old "exact pair" rule was a declarative-layer constraint (agentOptions custom keys are stripped by zod); the waterfall layer can rewrite the whole config (kix-cost's lite fallback proved the same seam). The candidate pool is the `llm-pi-ai:` list of `settings.yaml` plus the pi-ai built-in catalog; model-line upgrades only touch settings.
- **Cost tiers (v5.8)**: `subagent_lite` = slim composition (mechanical persona ~60 tokens shadows the ~17k resident persona; `toolFilter` trims 82 tools / 17.2k tokens down to read/grep/glob/pwsh / 1.6k tokens) → per-step fixed overhead ~34.3k → ~5.9k tokens (↓83%, measured). Preferred route `zai-coding-cn/glm-4.7` is **auto-probed** by `plugins/kix-cost.js` on the child's first request (`llm.listProviders` + `resolveModelInfo`); when unavailable (other environments) it falls back to the environment default route (`agentDefaultModel.currentSelection()`). `subagent_thinker` (deepseek-v4-flash, 128K cap) is the same-family max-effort tier.
- **Effort normalization (`plugins/kix-cost.js`)**: deepseek children without an explicit effort get `reasoningEffort` injected by budget cap — `subagent_thinker` (≥98K) → `max`, others (64K) → `high` (the adapter default is high/256K; the 64K cap is the runaway guard). GLM children are adapter-managed; the main session (explicit effort from `settings.yaml` `agent-default-model.reasoningEffort`, default `high`) and any explicit per-session selection are never touched. Runs on the `agent/request` waterfall; unit tests: `node plugins/kix-cost.test.js`.
- **Vision**: the main model declares no image input, so `read_image` is rejected by the routing gate. To inspect images (screenshots / error images / charts / UI / OCR) → pass the image path + question to `subagent_vision` (auto-routed to the first image-capable model).
- **UI image upload**: the profile plugin `dsh-vision-bridge` (installed by the npm package) converts pasted/dropped images to text descriptions at send time; messages arrive prefixed `📷 [图片自动识别]` — plugin output, not the user's words. If the description is insufficient, ask the user for the image path and use `subagent_vision`.
- Both `subagent_cross` and `subagent_vision` route through `llm-pi-ai.providers` in `settings.yaml` — see §Settings.

### §3.1 Subagent result-return channels (2026-08-17, field-tested)

Background (continuable) children have two return channels. Mechanism facts from dsh-subagent source (`notifySettlement` / `deliverReport`) + the dae review session:

| Channel | Triggered by | Semantics | Cost |
|---|---|---|---|
| `report` tool | the child, mid-run | Queues for a **later** parent turn only (does not conclude the child's turn); a mid-run relay | one injection per call |
| settlement notice `subagent-settled` | the runtime, at Activation settlement | **Unconditional**: end_turn / token ceiling / model failure / cancellation / teardown all notify; carries the stop reason + the full final assistant message | exactly one per child |

- **Wake mechanism**: idle parent → `followup` opens a new turn automatically; running parent → `steer` injects into the current turn. ⇒ **Ending the turn to wait has zero loss risk** (field-tested: post-final-turn report/settled each woke a short turn that completed integration).
- **Dual-channel redundancy (field-tested pitfall)**: a child that "`report`s the full text AND puts it in the final message again" costs the parent both injections + two wake turns (dsh-subagent README: *"A child that both reports and settles costs the parent both"*). Paradigm convention (in persona): **single-channel delivery** — the final message is the report; don't `report` the same text.
- **Sleep-wait anti-pattern (field-tested pitfall)**: 8× `sleep 45~240s` pinning the turn while waiting = pure wasted latency. Correct shape: independent work exhausted → short status → end the turn and wait for the wake. kix-orchestration v4 one-shot reminds on "bare sleep + description mentioning subagent" (0% false positives: backoff/lock-wait sleeps don't match; v4.1 is platform-agnostic — both command shapes always tested on any tool's `command`).

### §3.2 Arranger model & member menu (2026-08-17, four-round convergence)

The DSH landing of the CEO "pick members" power: **the main model is the arranger; members are activatable tiers**. The fixed producer→dev→qa pipeline is a kixpower sprint-workflow convention (heavy path), not a paradigm invariant — S7 (CEO-direct) / S8/S9 (dev-only) already validated free composition; this section makes it explicit (rules are liabilities: removing a fixed rule nobody followed is debt repayment).

| Member tier | Name handle | Contract (distilled from agents/*.agent.md — the file stays the single source of truth) | Activation |
|---|---|---|---|
| `subagent_dev` | Nova/Sage/Milo three-in-one | code per plan, write inside target_rules, no scope creep, never sign for QA; run deterministic gates per task | auto-activate on first use (kix_capability_call mounts) / kix_tool_activate pre-activates |
| `subagent_qa` | Ivy | never business source code, evidence gate, signoff artifacts (PASS/CONDITIONAL/FAIL/REVERIFY_REQUIRED evidence-bound) | auto-activate on first use (kix_capability_call mounts) / kix_tool_activate pre-activates |
| `subagent_reviewer` | unnamed | read-only + three-layer devil's advocate (L1 rebuttal rehearsal / L2 depth probe / L3 language-model stress) + rebuttal output | auto-activate on first use (kix_capability_call mounts) / kix_tool_activate pre-activates |

**Three deliberate non-rows**: no producer row (S7 proved CEO self-planning usually suffices; for Remy-grade planning the main thread reads `agents/kixpower-producer.agent.md` once); no orchestrator row (coordination stays in the main thread — the DSH main agent has the full orchestration toolset; materializing a coordinator subagent = hiring someone to coordinate yourself); dev as one three-in-one tier (Nova/Sage/Milo switching already lives in one agent body).

**Four floor invariants** (free composition never erodes; see the persona Arranger Model section): ① observation independence (phase duality) — you compose hands, eyes never self-attest; ② coordination stays in the main thread; ③ perspectives come from prompts, not personification — names are contract handles, light-path observers keep unnamed prompts (two-layer menu); ④ the gate floor is composition-independent (human confirmation before publish/merge/destructive, kix-guards/kix-discipline unchanged, team output is still claims).

**Drift antidotes (no new gates)**: say the composition out loud (who + why) + persist it in the `mode` field of `kix_discipline_spec` (members + one-line reason, added 2026-08-17, optional) + observe the composition distribution under rules-are-liabilities (over-composition and default-solo both get harvested). Mid-flight mis-composition → re-route once and say it.

## §4 Settings.yaml (host plane — the preset cannot install it)

`~/.dsh/settings.yaml` must contain, under `llm-pi-ai.providers`:

- `zai-vision`: GLM-4.6V vision provider — `api: openai-completions`, `baseURL: https://open.bigmodel.cn/api/coding/paas/v4`, models `glm-4.6v` / `glm-4.6v-flash` / `glm-4.5v` each with `input: [text, image]`; API key via env `ZAI_CODING_CN_API_KEY` or `~/.dsh/.credentials.yaml`.
- `zai-coding-cn`: GLM-5.3 cross-vendor observer (text) — `models` list declares glm-5.3 / glm-5.2 / glm-5.1 / glm-5-turbo / glm-4.7 / glm-4.5-air.

Without these, `subagent_cross` / `subagent_vision` cannot route. The installer (`kixparadigm-en`) reports this checklist; `kixparadigm-en doctor` re-checks it.

## §5 Native orchestration

- Complex multi-stage orchestration → `workflow` (phases/pipeline/parallel); long-horizon goals → `goal`; planning first → plan mode; long tasks → `pwsh run_in_background` + `job_output`.
- Full-flow suggestions (new project / full PR review / continue sprint) → suggest the matching `/kixpower-*` command, wait for user confirmation, never auto-trigger.

## §6 PTC / Code Mode (both enabled — red lines)

- `run_code` is for mechanical multi-step / batch / concurrent read-only probes; **verification and observation stay native** (evidence must be replayable, gates visible one by one).
- Red lines: verification evidence must be re-run natively or handed to an independent observer; the program must print all context later steps need (including denied sub-dispatches and reasons); on denial, stop immediately — never silently swallow a denial; intermediate values do not flow back.

## §7 Known translation scope

`skills/`, `prompts/`, `memories/`, and team-role `agents/` are still the Chinese originals in this edition (fully functional; terminology bridge in `instructions/glossary.md`). See `TRANSLATION-STATUS.md`. The full Chinese adaptation history (incl. vision-bridge troubleshooting) lives in the `kixparadigm` (CN) preset's `DSH-ADAPTATION.md`.
