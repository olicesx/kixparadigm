# The `kixparadigm` agent preset: the full coding agent, running on the kix
# self-orchestration paradigm.
#
# kix 分层：
#   - 常驻认知层（本文件 persona）——三通道交叉验证、阶段二相性、规则是负债、
#     需求三检、写码前决策链、AI 盲点补足、流程路由信号、编曲模型
#     （2026-08-17 起：主模型自由挑成员+流程，四条不变量地板见 persona）。
#   - 机制细节层（按需技能）——本 preset 的 skills/ 目录：kixparadigm（机械门禁、
#     验证 gate、输出格式、跨厂商模型）、kixpower（多智能体编排 v5.7）、handoff、
#     write-a-skill、improve-codebase-architecture、pwsh-reliable（Windows 命令
#     可靠性：native 参数/引号/退出码/WSL/清理，2026-08-17 吸收）。
#   - 团队角色（agents/）、kixpower 流程（prompts/）、方法论记忆（memories/）、
#     核心指令原件（instructions/）作为 preset 内资源随附。
#
# 来源：kix-bundle（VS Code Copilot 定制包）导入 DeepSeek Harness。工具名已映射：
#   runSubagent → subagent / subagent_fork；vscode_askQuestions → ask_user_question；
#   read_file/grep_search/replace_string_in_file/run_in_terminal → read/grep/edit/pwsh；
#   VS Code PreToolUse hooks → 本 harness 的 sandbox + approval 栈（kixpower 的
#   hooks/*.ps1 脚本仍随技能目录保留，可经 pwsh 手动调用做机械检查）。
#
# 本文件是 AGENT-PLANE 组成。roster 在 standing scope 下挂载一次；命名它的每个
# 会话按 scope 血缘加入。Host 组成（base.cordis.yml + web.cordis.yml）保留 preset
# 不得拥有的东西：注册表本身、sandbox 与 approval 栈、持久化、模型路由。
#
# 发布服务的行必须位于带 `isolate` realm 的 group 内，否则发布进 root realm
# 即进程全局——另一 preset 同名发布会冲突；`dsh-agent-presets` 在挂载时拒绝。
# `true` 表示 entry-local realm：本 standing mount 自己的私有实例。

# ── identity ────────────────────────────────────────────────────────────────

# kixparadigm 常驻认知层（每次会话生效）。`{{model}}`/`{{cwd}}` 从 agent 自己的
# 路由与工作区解析。
- id: persona
  name: '@deepseek-ai/dsh-persona'
  disabled: true
  config:
    text: &kix_persona |-
    prefix: *kix_persona
- id: persona-incentive
  name: '@deepseek-ai/dsh-persona'
  config:
    text: &kix_persona_incentive |-
      You are a coding agent powered by the {{model}} model, running on the DeepSeek Harness. Your working directory is {{cwd}}.

      Preserve safety, permissions, user intent, and evidence integrity.
    prefix: *kix_persona_incentive
- id: agent-instructions
  name: '@deepseek-ai/dsh-agent-instructions'
  config:
    maxBytes: 65536

# ── shell ───────────────────────────────────────────────────────────────────

# `shell-env` stays in the HOST composition: `apps/cli/src/web.ts` injects it to
# publish `DSH_WEB_URL`/`DSH_WEB_MODE`, and a host row that injects a service is
# the criterion for host-plane ownership — injection resolves before any session
# exists, so there is no agent to key by. Behind a preset realm those variables
# never reached the model's shell at all. Both shell tools consume the host
# registry from here; their executors (`bash-sandbox`/`pwsh-sandbox`) are
# host-plane too.
- id: tool-bash
  name: '@deepseek-ai/dsh-tool-bash'
  disabled: !!js process.platform === 'win32'

- id: tool-pwsh
  name: '@deepseek-ai/dsh-tool-pwsh'
  disabled: !!js process.platform !== 'win32'

# ── filesystem ──────────────────────────────────────────────────────────────

# Both register into the host `tools` registry and provide nothing, so
# they need no realm. The `fs` service and its policy stay in the host.
- id: tool-fs
  name: '@deepseek-ai/dsh-tool-fs'

- id: tool-fs-search
  name: '@deepseek-ai/dsh-tool-fs-search'
  config:
    sampleOverCapGlobResults: false

# ── background jobs ────────────────────────────────────────────────────────

# Only the model-facing controls. The task REGISTRY stays on the host plane:
# its producers sit outside any realm this file could put it in — `tool-bash`
# above resolves it with `ctx.get`, and an entry-local realm here is invisible
# to every sibling row. The registry is keyed by owning agent anyway, so one
# host instance serves every session. What a preset chooses is whether its
# agent can collect and stop background work at all.
# 2026-08-16 曾默认 disabled（渐进面方案 A），实测教训：disabled 时后台任务
# 连启动都失败——`run_in_background` 直接报 "background jobs unavailable: no
# job controller serves this agent (load @deepseek-ai/dsh-tool-jobs in its
# composition)"，persona 教的「长任务 run_in_background+job_output」与组成
# 互相矛盾。2026-08-17 决策（用户原则：简单机械、不影响思考的工具常驻）：
# job_* 是纯机械控制面（启动/回收/停止已跑任务，不引入决策负担）→ 常驻启用。
# goal 与低频 subagent 档位仍默认 disabled，由 kix-focus 首用自动激活；
# workflow/job_* 及 reviewer/dev/qa 高频成员常驻。
- id: tool-jobs
  name: '@deepseek-ai/dsh-tool-jobs'

# ── skills ──────────────────────────────────────────────────────────────────

# The skill REGISTRY lives in the host composition and is layered per scope:
# these rows register into THIS preset's layer of it, so they need no realm.
# `skill-filesystem` contributes local-root discovery for agents on this preset —
# including this preset's own `skills/` directory, which carries the kix skills —
# and `tool-skill` gives them the catalog and loader. The merged catalog also
# carries whatever the deployment registered globally (repository plugins).
# `baseUrl` is this preset's own directory, so the root resolves wherever the
# preset is installed.
- id: skill-filesystem
  name: '@deepseek-ai/dsh-skill-filesystem'
  disabled: true
  config:
    customSkillDirs:
      - !!js "process.getBuiltinModule('node:url').fileURLToPath(new URL('skills/', baseUrl))"

- id: tool-skill
  name: '@deepseek-ai/dsh-tool-skill'

# ── goals ───────────────────────────────────────────────────────────────────

# Only the model-facing tool. The goal SERVICE, its session driver, and the
# `/goal` command stay on the host plane: the Gateway serves the goal domain as
# Remote endpoints whose receiver comes from a generated descriptor, so it
# resolves `goals` on the host and an entry-local realm here would hide it. The
# registry is keyed by session anyway, so one host instance serves every
# session. What a preset chooses is whether its agent can call the goal tool.
# 2026-08-15 精简决策：goal 低频重型 → 默认 disabled（主 agent 工具面只留
# 范式必需；restrict 裁不到 scope 工具）。2026-08-17 起首次使用自动激活：
# kix_capability_call { tool: create_goal } 即挂载并执行（或 kix_tool_activate
# { tool: goal } 显式预激活），无需重启。
- id: tool-goal
  name: '@deepseek-ai/dsh-tool-goal'
  disabled: true

# ── plan mode ───────────────────────────────────────────────────────────────

# Plan state is per-agent by nature, so an entry-local realm is not a
# workaround here — it is the correct lifetime.
- id: planning
  name: cordis:group
  group: true
  isolate:
    planMode: true
  config:
    - id: plan-mode
      name: '@deepseek-ai/dsh-plan-mode'
      config:
        section: |
              You are in plan mode. Stay in plan mode until exit_plan_mode succeeds or the user switches the session mode. Imperative language to implement changes means plan the implementation, not execute it. A user's conversational agreement — including an answer confirming something you asked — approves nothing and does not end plan mode; fold the confirmed decision into the plan and submit it through exit_plan_mode.

              Explore first. Use non-mutating reads, searches, static analysis, and checks to ground the plan in the actual repository. Do not edit or write files, change configuration, run formatters or code generation that rewrites tracked files, commit, or otherwise carry out the plan. Prefer existing functions and patterns over new machinery.

              The tool catalog stays the same across modes for request-cache stability. These plan-mode rules override any later tool description or guidance that suggests using mutation tools; those tools remain listed to keep the tool catalog unchanged. Do not use todo_write to track this planning phase: it tracks implementation after an approved plan, while the plan itself belongs in exit_plan_mode.

              Resolve discoverable facts by inspection. Use ask_user_question only for user-owned choices or material ambiguity that inspection cannot answer. Do not ask the user where code lives or how current behavior works when you can find out.

              Make the plan decision-complete: state the goal and success criteria; group implementation changes by subsystem; identify public API, schema, and data-flow changes; cover edge cases, failure modes, tests, acceptance criteria, and explicit assumptions. Keep it concise enough to review but detailed enough that another engineer can implement it without making design decisions.

              When ready, call exit_plan_mode with the complete plan markdown, starting with a # title. Make exit_plan_mode the only and final tool call in that assistant response: it presents the plan for approval, and implementation begins only in a later step after approval. Do not paste the final plan as a plain reply or ask "should I proceed?" through prose or ask_user_question. If review rejects it, incorporate the feedback and present again. If the review channel is unavailable or aborted, stay in plan mode and ask the user to switch modes manually; do not proceed with implementation.

# ── compaction ──────────────────────────────────────────────────────────────

# `compaction-basic` reads `toolResultPrune` through `ctx.get`, so the pruner must
# share this realm rather than sit outside it.
#
# `tokenMeter` is deliberately NOT in this realm: the meter stays on the HOST
# plane, and the rows here resolve that one instance. It takes no configuration,
# keys every fold by Session, and owns the context-meter projection units the
# browser reads for every session — behind a realm those units would come and go
# with whichever presets happen to be mounted. What a preset chooses is whether
# its agent compacts at all, which is `compaction-basic` below.
- id: compaction
  name: cordis:group
  group: true
  isolate:
    compaction: true
    toolResultPruner: true
  config:
    - id: compaction-basic
      name: '@deepseek-ai/dsh-compaction-basic'
      config:
        # 甜点策略：阈值 = 上限 × 每模型溢出安全，保留预算同样封顶
        #     T = min(maxThresholdTokens 200K, 0.8 × W)
        #     R = min(maxRetainTokens   64K, 0.044 × W)
        # 64K–4M 任何窗口自动落到甜点，新模型/新厂商零配置适配，故 modelPolicies 为空。
        #
        # 依赖 kix-compaction-cap-patch.mjs 给引擎加的两个绝对字段；未打补丁的进程会
        # 因未知配置键中止挂载。dsh-web.service 的 drop-in kix-cap-patch.conf 在每次
        # 启动时自愈补丁（防 dsh 升级覆盖 node_modules）。落地/回滚流程见
        # docs/kix-context-management.md，脚本在 scripts/context-budget/。
        #
        # 血统：默认 0.8 在 1M = 800K 阈值，kixpower 整改会话爬到 466K 从未触发，上下文
        # O(N²) 累积烧掉 100.8M 重读 token；v5.9 全局降到 0.45 并对两个 1M 模型钉 0.18。
        # 但 0.45 在 1M=450K 仍从不管，而 0.18(180K) 与继承的 retain 0.16(160K) 只剩 20K
        # 可压区间 → session-c3370a1e 在 21 步里触发 41 次压缩、每次对 prompt 中位变化
        # +50 token（等于没压）。2026-09-12 本机标定（8-needle + 序数检索）：deepseek-flash
        # / glm-5.3 在 20K–400K 全对，未测到拐点；同负载 27 请求下 200K 上限 = 2.63M/2.80M
        # 累计 prompt、256K = 3.57M、400K = 4.68M，三者召回与压缩保真全 PASS → 取三条独立
        # 约束（成本随上限单调 / 可压带 R ≤ 0.4(T−F)，F≈23K / 有效利用 ~20%）的交点 200K。
        thresholdRatio: 0.8
        maxThresholdTokens: 200000
        retainRatio: 0.044
        maxRetainTokens: 64000
        modelPolicies: []

    - id: command-compact
      name: '@deepseek-ai/dsh-command-compact'

    - id: tool-result-pruner
      name: '@deepseek-ai/dsh-compaction-tool-result-pruner'
      config:
        thresholdChars: 2048
        headChars: 1024
        tailChars: 512

    # v5.9 主会话预算（㉑-㉓）：advisory + 急剪。必须挂本 realm 内——
    # ctx.get('toolResultPruner') 只在 isolate realm 可见（compaction-basic
    # 同款约束）；tokenMeter/llm 留宿主面，realm 内照常解析。
    - id: kix-budget
      name: ./plugins/kix-budget.js
      disabled: true

# ── delegation and workflows ────────────────────────────────────────────────

# The `subagents` registry and its spawn/fork backends live in the HOST
# composition: the registry is a process singleton whose cross-session queries
# the api-proxy serves to the browser, and a provider name may only be
# registered once. This preset contributes the delegation TOOLS, which resolve
# that host registry. kixparadigm 三通道观察与 CEO 团队编排正使用它们。
#
# `workflows` is different — nothing outside an agent reads it — so every row
# that reaches it shares one entry-local realm here, and a consumer left
# outside would resolve a host registry this preset does not populate.
#
# `tool-subagent-report` is host-plane for the same reason as the registry,
# not because a preset may not want it: it registers a CONTINUABLE SETUP on
# that singleton rather than a tool this agent calls, and the setup list is
# not scope-aware — one copy per mounted preset means every child gets
# `report` registered once per live session, which throws on the second.
- id: delegation
  name: cordis:group
  group: true
  isolate:
    workflowEngine: true
  config:
    - id: tool-subagent-control
      name: '@deepseek-ai/dsh-tool-subagent-control'

    - id: tool-subagent-list-agents
      name: '@deepseek-ai/dsh-tool-subagent-control/list-agents'

    - id: tool-subagent
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: spawn
        toolName: subagent
        backgroundMode: continuable
        maxDepth: 2
        # ── 原生模型选型（2026-09-10）──────────────────────────────────────
        # 融合而非并列：通用档改用 DSH 原生能力——本行每次调用可显式给
        # provider / model / reasoning_effort，并注册原生发现工具
        # list_subagent_models（返回真实目录里的 provider、模型与 reasoning
        # efforts）。省掉「另配一套目录 API」的必要。
        # 为何只在本行开：list_subagent_models 是全局工具名，同一 scope 内
        # 只能有一个实例拥有选型（dsh-tool-subagent README「At most one
        # instance ... may own model selection」）。三个路由档位继续用
        # kix-route 哨兵做**默认值**（保证不退化），需要跨档位显式选型时走
        # 通用档 + 参数。
        # 生效前提（两处都在）：host 挂 @deepseek-ai/dsh-tool-subagent/
        # model-selection-settings；settings.yaml 的 subagent-model-selection
        # .enabled=true 且 allowedModels 非空（空白名单会在挂载时 throw）。
        modelSelectionSettings: true
        # v5.10（2026-08-17 子代理账本实测：107 子代理 2,432 次调用，82% 是
        # bash/read/grep/glob；exit_plan_mode/ask_user_question/
        # kix_tool_activate/kix_capability_call 全部 0 使用）——子代理编排面
        # 裁剪。deny 名单只含 8 个恒注册编排名（restrict 对未注册名抛错）；
        # subagent 移出名单：dsh-subagent 用 composeFrom(childCtx, parent.ctx)
        # 把 preset 挂进**子代理自己的 layer**，own-layer 名不在
        # restrictableNames 内，留在名单里 = 每次派发 throw
        # （tools.restrict() names unknown global tool "subagent"）。
        # 嵌套派发改由 plugins/kix-cost.js 的 tools.guard + harness maxDepth
        # 拒绝。条件挂载名由 plugins/kix-cost.js 分层：仅 depth-1 subagent_lite
        # 机械取证放行，其余 tier/goal/workflow 拒绝。deny 名同时从 tools 与 TypeScript
        # 工具镜像消失。run_code 不在名单：它是 Code Mode 保留传输名，
        # restrict 不能命名（WSL2 实弹实锤），作为正当执行通道留在子代理
        # 可见面，其工具镜像一并保留。subagent×2/agent 控制是「编排
        # 留主线程」（kix 地板②）的主线程职责。文件/执行/检索/jobs/
        # skill/report/MCP（capability_call）全保留——裁的是子代理定义上
        # 不该有的编排面，不是能力。
        toolFilter:
          deny: [ exit_plan_mode, subagent_cross, interrupt_agent, send_message, list_agents, ask_user_question, kix_tool_activate, kix_tool_deactivate ]
        agentOptions:
          # v5.8 预算帽：完成侧（思考+回答）兜底上限，防失控（s1 budget forcing）
          maxTokens: 65536

    # ── 低成本例行子代理（v5.8 成本分层 = DSH 精简模式）─────────────────
    # 机械读码/格式检查/简单检索 → `subagent_lite`（精简组合）：
    #   persona 字段把 17k token 的 kix 常驻人格替换为 ~60 token 的机械人格
    #   （per-child persona shadowing）；toolFilter 把全部工具 schema
    #   裁剪到 read/grep/glob + 平台 shell 四个（1.6k token）——每步固定开销从
    #   ~34.3k 降到 ~5.9k（实测数据，↓83%）。2026-08-17 WSL2 E2E 修复：原
    #   硬编码 pwsh（Windows 工具）在 Linux 部署 tools.restrict() 报 unknown
    #   global tool 致 lite 档不可用 → 改平台条件化（!!js process.platform）。
    #   模型档：**不钉路由**——agentOptions 不含 provider/model，子代理按 spawn
    #   provider 的继承语义直接用父代理路由（本机默认 deepseek-flash / high），
    #   跨环境分发天然可用。lite 与普通档的区别在 persona（~60 token 机械人格）
    #   + toolFilter（4 工具）+ 8K 预算帽（`plugins/kix-cost.js` isLiteTier），
    #   不是「换个厂牌」，因此不依赖任何特定廉价模型是否被注册。
    #   2026-09-10 变更前此处为 zai-coding-cn/glm-4.7。
    #   机械判定：只读/检索/核对，无需判断、无需写文件；否则用 `subagent`。
    # 普通分派 → `subagent`（继承主模型，high，64K 帽）；
    # 高难/跨厂商验证 → `subagent_cross`（自动取反厂商，64K 帽）；
    # 需 max 深度思考的同族任务 → `subagent_thinker`（deepseek max，128K 帽，
    #   由 plugins/kix-cost.js 按预算帽判别注入 max）。
    # vision 由模型配置 4096 封顶。子代理思考强度由 plugins/kix-cost.js 统一注入。
    # 2026-08-16 渐进面（方案 A）细分档位默认 disabled；2026-08-17 决策
    # （用户原则：有认知负担的工具用机制化自动激活）→ 首次使用自动激活：
    # kix_capability_call { tool: subagent_lite, arguments: {...} } 即挂载并
    # 执行（inject 宿主服务，动态挂载可行，与 goal 同机制），下一轮可直呼；
    # 也可 kix_tool_activate 显式预激活。subagent/subagent_cross 常驻（三通道核心）。
    - id: tool-subagent-lite
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: spawn
        toolName: subagent_lite
        backgroundMode: continuable
        maxDepth: 2
        persona: |-
          You are a fast mechanical subagent for strictly mechanical subtasks:
          reading files, searching, simple checks and verification. Do exactly
          what the task asks. No extra analysis, no suggestions, no speculation.
          Return concise factual results with file:line evidence when relevant.
        toolFilter:
          allow: [ read, grep, glob, !!js "process.platform === 'win32' ? 'pwsh' : 'bash'" ]
        agentOptions:
          # 不钉 provider/model：子代理默认继承父代理路由（spawn provider 的
          # 继承语义，见 dsh-subagent-spawn-in-process README「inherits the
          # parent's ... provider, model, reasoning effort」）。lite 档的区分度
          # 由 8K 预算帽承载（plugins/kix-cost.js isLiteTier = maxTokens<=8192），
          # 不依赖某个特定厂商模型存在。2026-09-10：原钉 zai-coding-cn/glm-4.7
          # 在本机未注册（catalog 只有 glm-5.3 / glm-5.3-flash）→ 机械档不可用。
          maxTokens: 8192

    # ── max 深度思考档（v5.8）：该用 max 的任务仍用 max ──────────────────
    # 同族深度思考档：kix-route 哨兵 `kix-route:thinker` 在首次请求时解析到
    # deepseek 系最新可用模型（deepseek-official 首选，其余 deepseek-* 兜底）；
    # deepseek 全缺席的部署 → 降级环境默认路由并告警（thinker 角色仍成立：
    # 大预算深思考，GLM 适配器自管 effort）；effort 由 kix-route 注入（复用
    # kix-cost 同源的 decideEffort，按预算帽 128K → max；kix-cost 本体对哨兵
    # child 只守卫跳过、不注入——两插件任意 waterfall 顺序结果一致）。用于需要同族深度思考的高难任务；
    # 跨厂商正交验证用 `subagent_cross`（自动取反厂商）。2026-08-16 渐进面：
    # thinker 默认 disabled，首次使用自动激活（kix_capability_call 挂载）。
    - id: tool-subagent-thinker
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: spawn
        toolName: subagent_thinker
        backgroundMode: continuable
        maxDepth: 2
        # v5.10（2026-08-17 子代理账本实测：107 子代理 2,432 次调用，82% 是
        # bash/read/grep/glob；exit_plan_mode/ask_user_question/
        # kix_tool_activate/kix_capability_call 全部 0 使用）——子代理编排面
        # 裁剪。deny 名单只含 8 个恒注册编排名（restrict 对未注册名抛错）；
        # subagent 移出名单：dsh-subagent 用 composeFrom(childCtx, parent.ctx)
        # 把 preset 挂进**子代理自己的 layer**，own-layer 名不在
        # restrictableNames 内，留在名单里 = 每次派发 throw
        # （tools.restrict() names unknown global tool "subagent"）。
        # 嵌套派发改由 plugins/kix-cost.js 的 tools.guard + harness maxDepth
        # 拒绝。条件挂载名由 plugins/kix-cost.js 分层：仅 depth-1 subagent_lite
        # 机械取证放行，其余 tier/goal/workflow 拒绝。deny 名同时从 tools 与 TypeScript
        # 工具镜像消失。run_code 不在名单：它是 Code Mode 保留传输名，
        # restrict 不能命名（WSL2 实弹实锤），作为正当执行通道留在子代理
        # 可见面，其工具镜像一并保留。subagent×2/agent 控制是「编排
        # 留主线程」（kix 地板②）的主线程职责。文件/执行/检索/jobs/
        # skill/report/MCP（capability_call）全保留——裁的是子代理定义上
        # 不该有的编排面，不是能力。
        toolFilter:
          deny: [ exit_plan_mode, subagent_cross, interrupt_agent, send_message, list_agents, ask_user_question, kix_tool_activate, kix_tool_deactivate ]
        agentOptions:
          model: kix-route:thinker
          maxTokens: 131072

    # ── 跨厂商观察者（kixparadigm 三通道验证的 DSH 落点）────────────────────
    # 命名钉角色不钉厂商（cross = 主厂商取反者），路由也不钉厂商：
    # agentOptions.model = `kix-route:cross` 哨兵，由 plugins/kix-route.js 在
    # 子代理首次请求时按运行时目录（llm.listProviders/listModels）解析成
    # 「与父模型不同厂商」的第一个可用模型（父 GLM → deepseek 系；父
    # DeepSeek → zai 系；偏好序见插件 MODEL_PREFERENCE）。主模型换厂商、
    # 模型线升级、分发到其他部署都不需要改本行。边界语义（v5.9.1）：
    # **单厂商部署**（无任何异厂商 provider 注册）时启动即报错——错误信息
    # 附已注册清单与改用建议，随 run 失败带回父模型（改用 subagent 同厂商
    # 复核 + 注明局限 / 配置第二厂商）。绝不静默同厂商降级：cross 的全部
    # 价值是厂商正交，假独立性比失败更糟。
    # 插件缺失时哨兵名直达适配器 → UNKNOWN_MODEL 响亮失败，不静默降级。
    # v5.8：maxTokens 64K 兜底帽。
    - id: tool-subagent-cross
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: spawn
        toolName: subagent_cross
        backgroundMode: continuable
        maxDepth: 2
        # v5.10（2026-08-17 子代理账本实测：107 子代理 2,432 次调用，82% 是
        # bash/read/grep/glob；exit_plan_mode/ask_user_question/
        # kix_tool_activate/kix_capability_call 全部 0 使用）——子代理编排面
        # 裁剪。deny 名单只含 8 个恒注册编排名（restrict 对未注册名抛错）；
        # subagent 移出名单：dsh-subagent 用 composeFrom(childCtx, parent.ctx)
        # 把 preset 挂进**子代理自己的 layer**，own-layer 名不在
        # restrictableNames 内，留在名单里 = 每次派发 throw
        # （tools.restrict() names unknown global tool "subagent"）。
        # 嵌套派发改由 plugins/kix-cost.js 的 tools.guard + harness maxDepth
        # 拒绝。条件挂载名由 plugins/kix-cost.js 分层：仅 depth-1 subagent_lite
        # 机械取证放行，其余 tier/goal/workflow 拒绝。deny 名同时从 tools 与 TypeScript
        # 工具镜像消失。run_code 不在名单：它是 Code Mode 保留传输名，
        # restrict 不能命名（WSL2 实弹实锤），作为正当执行通道留在子代理
        # 可见面，其工具镜像一并保留。subagent×2/agent 控制是「编排
        # 留主线程」（kix 地板②）的主线程职责。文件/执行/检索/jobs/
        # skill/report/MCP（capability_call）全保留——裁的是子代理定义上
        # 不该有的编排面，不是能力。
        toolFilter:
          deny: [ exit_plan_mode, subagent_cross, interrupt_agent, send_message, list_agents, ask_user_question, kix_tool_activate, kix_tool_deactivate ]
        agentOptions:
          model: kix-route:cross
          maxTokens: 65536

    # ── 识图补充（视觉子代理，自动路由到 image 模型）────────────────────
    # 主模型不声明 image 输入时，read_image 的路线门禁会拒绝直接看图。
    # 需要识图时把图片路径 + 具体问题交给 subagent_vision。路由不再钉值：
    # agentOptions.model = `kix-route:vision` 哨兵，由 plugins/kix-route.js
    # 在子代理首次请求时解析到第一个「inputModalities 声明 image」的可用
    # 模型（zai-vision 偏好序：glm-4.6v → glm-4.6v-flash → glm-4.5v，其余
    # provider 兜底）；门禁读 waterfall 后的真实路由（requestHeader 配置）
    # → 改写后放行。边界（v5.9.1）：全目录无 image 模型 → 启动即报错（附
    # 配置建议），不做无视觉能力的降级（省掉 spawn→read_image 被弹回的空跑）。
    # 2026-08-16 渐进面：vision 默认 disabled，首次使用自动激活（kix_capability_call 挂载）。
    - id: tool-subagent-vision
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: spawn
        toolName: subagent_vision
        backgroundMode: continuable
        maxDepth: 2
        # v5.10（2026-08-17 子代理账本实测：107 子代理 2,432 次调用，82% 是
        # bash/read/grep/glob；exit_plan_mode/ask_user_question/
        # kix_tool_activate/kix_capability_call 全部 0 使用）——子代理编排面
        # 裁剪。deny 名单只含 8 个恒注册编排名（restrict 对未注册名抛错）；
        # subagent 移出名单：dsh-subagent 用 composeFrom(childCtx, parent.ctx)
        # 把 preset 挂进**子代理自己的 layer**，own-layer 名不在
        # restrictableNames 内，留在名单里 = 每次派发 throw
        # （tools.restrict() names unknown global tool "subagent"）。
        # 嵌套派发改由 plugins/kix-cost.js 的 tools.guard + harness maxDepth
        # 拒绝。条件挂载名由 plugins/kix-cost.js 分层：仅 depth-1 subagent_lite
        # 机械取证放行，其余 tier/goal/workflow 拒绝。deny 名同时从 tools 与 TypeScript
        # 工具镜像消失。run_code 不在名单：它是 Code Mode 保留传输名，
        # restrict 不能命名（WSL2 实弹实锤），作为正当执行通道留在子代理
        # 可见面，其工具镜像一并保留。subagent×2/agent 控制是「编排
        # 留主线程」（kix 地板②）的主线程职责。文件/执行/检索/jobs/
        # skill/report/MCP（capability_call）全保留——裁的是子代理定义上
        # 不该有的编排面，不是能力。
        toolFilter:
          deny: [ exit_plan_mode, subagent_cross, interrupt_agent, send_message, list_agents, ask_user_question, kix_tool_activate, kix_tool_deactivate ]
        agentOptions:
          model: kix-route:vision
          # 视觉模型普遍 4K 输出帽（settings zai-vision maxTokens 4096）；
          # 显式钉值同时防止 kix-cost 把无 maxTokens 的 child 误判为 lite 档
          maxTokens: 4096

    # 2026-08-16 渐进面：fork 默认 disabled，首次使用自动激活（kix_capability_call 挂载）。
    - id: tool-subagent-fork
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: fork
        toolName: subagent_fork
        backgroundMode: continuable
        maxDepth: 2
        # v5.10（2026-08-17 子代理账本实测：107 子代理 2,432 次调用，82% 是
        # bash/read/grep/glob；exit_plan_mode/ask_user_question/
        # kix_tool_activate/kix_capability_call 全部 0 使用）——子代理编排面
        # 裁剪。deny 名单只含 8 个恒注册编排名（restrict 对未注册名抛错）；
        # subagent 移出名单：dsh-subagent 用 composeFrom(childCtx, parent.ctx)
        # 把 preset 挂进**子代理自己的 layer**，own-layer 名不在
        # restrictableNames 内，留在名单里 = 每次派发 throw
        # （tools.restrict() names unknown global tool "subagent"）。
        # 嵌套派发改由 plugins/kix-cost.js 的 tools.guard + harness maxDepth
        # 拒绝。条件挂载名由 plugins/kix-cost.js 分层：仅 depth-1 subagent_lite
        # 机械取证放行，其余 tier/goal/workflow 拒绝。deny 名同时从 tools 与 TypeScript
        # 工具镜像消失。run_code 不在名单：它是 Code Mode 保留传输名，
        # restrict 不能命名（WSL2 实弹实锤），作为正当执行通道留在子代理
        # 可见面，其工具镜像一并保留。subagent×2/agent 控制是「编排
        # 留主线程」（kix 地板②）的主线程职责。文件/执行/检索/jobs/
        # skill/report/MCP（capability_call）全保留——裁的是子代理定义上
        # 不该有的编排面，不是能力。
        toolFilter:
          deny: [ exit_plan_mode, subagent_cross, interrupt_agent, send_message, list_agents, ask_user_question, kix_tool_activate, kix_tool_deactivate ]
        agentOptions:
          # v5.8 预算帽：继承会话上下文的子代理同样兜底
          maxTokens: 65536

    # ── 反方辩护审查者（2026-08-16，三通道对抗性观察通道）───────────────
    # kixpower-reviewer 角色的机制化落点（此前仅 agents/ 模板 + review prompt
    # 按需加载）：只读 + 反方辩护三层（2026-08-17 三问显式分层）+ 结构化
    # rebuttal 输出。「结论发布前」的对抗性检查通道（对照：kix-guards 管操作
    # 前、kix-discipline 管编辑前）。现为常驻高频观察成员；重大审查可按不同
    # lens 并发多个 reviewer 实例。核心语义来自 agents/kixpower-reviewer.agent.md
    #（保持单一权威，此处为 DSH 可注入的精简 persona 快照）。
    - id: tool-subagent-reviewer
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: spawn
        toolName: subagent_reviewer
        backgroundMode: continuable
        maxDepth: 2
        # v5.10（2026-08-17 子代理账本实测：107 子代理 2,432 次调用，82% 是
        # bash/read/grep/glob；exit_plan_mode/ask_user_question/
        # kix_tool_activate/kix_capability_call 全部 0 使用）——子代理编排面
        # 裁剪。deny 名单只含 8 个恒注册编排名（restrict 对未注册名抛错）；
        # subagent 移出名单：dsh-subagent 用 composeFrom(childCtx, parent.ctx)
        # 把 preset 挂进**子代理自己的 layer**，own-layer 名不在
        # restrictableNames 内，留在名单里 = 每次派发 throw
        # （tools.restrict() names unknown global tool "subagent"）。
        # 嵌套派发改由 plugins/kix-cost.js 的 tools.guard + harness maxDepth
        # 拒绝。条件挂载名由 plugins/kix-cost.js 分层：仅 depth-1 subagent_lite
        # 机械取证放行，其余 tier/goal/workflow 拒绝。deny 名同时从 tools 与 TypeScript
        # 工具镜像消失。run_code 不在名单：它是 Code Mode 保留传输名，
        # restrict 不能命名（WSL2 实弹实锤），作为正当执行通道留在子代理
        # 可见面，其工具镜像一并保留。subagent×2/agent 控制是「编排
        # 留主线程」（kix 地板②）的主线程职责。文件/执行/检索/jobs/
        # skill/report/MCP（capability_call）全保留——裁的是子代理定义上
        # 不该有的编排面，不是能力。
        toolFilter:
          deny: [ exit_plan_mode, subagent_cross, interrupt_agent, send_message, list_agents, ask_user_question, kix_tool_activate, kix_tool_deactivate ]
        persona: |-
          You are kixpower-reviewer: an independent read-only adversarial reviewer.
          Hard constraints:
          - The bound artifact is read-only: never edit it, change its Git state,
            commit, push, or publish. Temporary reproducers outside artifact_root are allowed.
          - You may recursively dispatch narrow read-only evidence probes for a concrete
            information gap; propagate review_stage/review_policy/artifact_root to the tree.
          - Technical claims require evidence (file:line or official docs);
            unknown contracts return `unknown` — never escalate severity on assumption.
          Adversarial duty in three layers (devil's advocate; run all three
          before any claim is accepted):
          - L1 rebuttal rehearsal: what is the author's most likely technical
            rebuttal — intentional behavior or an explicit opt-in?
          - L2 depth probe: what is the DEEPEST property you verified — did you
            read the callee implementation, or stop at the surface call chain?
          - L3 language-model stress: for suggestions, do they hold under the
            target language's dispatch/type/concurrency model?
          Return structured output: for each claim, mechanism/contract/impact
          status (confirmed|disputed|unknown) with evidence, plus a `rebuttal`
          field with the author's most likely counterargument.
        agentOptions:
          maxTokens: 65536

    # ── 编曲成员档（2026-08-17，四轮碰撞收敛：主模型自由挑成员）─────────
    # 落地形态（DSH 最短路径）：主模型 = 编曲者；成员 = activatable 档，人名
    # 做契约句柄（"Ivy" 一个词绑定「不写业务源码+证据门禁+签署工件」这份契
    # 约），不是人设剧场——轻路径观察位仍用无名视角 prompt（kixparadigm
    # 「视角来自 prompt 不做角色化」不变量）。三项不建行决策：
    #   - producer 不建行：S7 已证 CEO 自规划通常够，真需要 Remy 级规划时
    #     主线程读一次 agents/kixpower-producer.agent.md 即可（低频不占菜单）；
    #   - orchestrator 不建行：协调留在主线程（DSH 主 agent 有全套编排工具，
    #     物化协调子代理 = 雇个协调员协调自己；636 行 orchestrator.agent.md
    #     是 Copilot 时代残留，不建行不蒸馏）；
    #   - dev 三人合一档：Nova/Sage/Milo 的内部切换本就发生在同一 agent
    #     body（「根据任务类型在三个角色间切换」），一行 persona 蒸馏契约即可。
    # 契约快照与 agents/kixpower-{qa,dev}.agent.md 保持单一权威关系（同
    # reviewer 先例）；组合留痕走 kix_discipline_spec 的 mode 字段（成员组合
    # + 一句理由）；四条不变量地板见 persona「编曲模型」节。dev/qa/reviewer
    # 常驻；职责命中优先专用成员，低频 lite/thinker/vision/fork 仍按需激活。
    - id: tool-subagent-qa
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: spawn
        toolName: subagent_qa
        backgroundMode: continuable
        maxDepth: 2
        # v5.10（2026-08-17 子代理账本实测：107 子代理 2,432 次调用，82% 是
        # bash/read/grep/glob；exit_plan_mode/ask_user_question/
        # kix_tool_activate/kix_capability_call 全部 0 使用）——子代理编排面
        # 裁剪。deny 名单只含 8 个恒注册编排名（restrict 对未注册名抛错）；
        # subagent 移出名单：dsh-subagent 用 composeFrom(childCtx, parent.ctx)
        # 把 preset 挂进**子代理自己的 layer**，own-layer 名不在
        # restrictableNames 内，留在名单里 = 每次派发 throw
        # （tools.restrict() names unknown global tool "subagent"）。
        # 嵌套派发改由 plugins/kix-cost.js 的 tools.guard + harness maxDepth
        # 拒绝。条件挂载名由 plugins/kix-cost.js 分层：仅 depth-1 subagent_lite
        # 机械取证放行，其余 tier/goal/workflow 拒绝。deny 名同时从 tools 与 TypeScript
        # 工具镜像消失。run_code 不在名单：它是 Code Mode 保留传输名，
        # restrict 不能命名（WSL2 实弹实锤），作为正当执行通道留在子代理
        # 可见面，其工具镜像一并保留。subagent×2/agent 控制是「编排
        # 留主线程」（kix 地板②）的主线程职责。文件/执行/检索/jobs/
        # skill/report/MCP（capability_call）全保留——裁的是子代理定义上
        # 不该有的编排面，不是能力。
        toolFilter:
          deny: [ exit_plan_mode, subagent_cross, interrupt_agent, send_message, list_agents, ask_user_question, kix_tool_activate, kix_tool_deactivate ]
        persona: |-
          You are kixpower-qa (Ivy): acceptance testing, bug filing, and QA
          signoff — never business source code.
          Hard constraints:
          - Edit only tests and QA docs (*_test.*, *.test.*, *.spec.*,
            *.stories.*, e2e/**, tests/**, cypress/**, docs/qa/*); bugs go to
            issues with repro steps, Dev fixes them — you re-test and close.
          - Deterministic-first: machine-verifiable gates (test/lint/typecheck/
            format) before any judgment call; never re-run with an LLM what a
            deterministic gate already covers.
          - Evidence gate: bug claims resting on external semantics (library/
            platform behavior) need evidence first (official docs / source
            file:line); unverified → "needs confirmation" — never inflate
            severity on assumption.
          - Signoff is evidence-bound: PASS/CONDITIONAL/FAIL only from executed
            gates + playthrough; incomplete evidence → no PASS; touching any
            test/fixture after verification → REVERIFY_REQUIRED, never a
            direct PASS; CONDITIONAL only means CI-pending.
          Report with tables: gate results / verdict / issue list.
        agentOptions:
          maxTokens: 65536

    - id: tool-subagent-dev
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: spawn
        toolName: subagent_dev
        backgroundMode: continuable
        maxDepth: 2
        # v5.10（2026-08-17 子代理账本实测：107 子代理 2,432 次调用，82% 是
        # bash/read/grep/glob；exit_plan_mode/ask_user_question/
        # kix_tool_activate/kix_capability_call 全部 0 使用）——子代理编排面
        # 裁剪。deny 名单只含 8 个恒注册编排名（restrict 对未注册名抛错）；
        # subagent 移出名单：dsh-subagent 用 composeFrom(childCtx, parent.ctx)
        # 把 preset 挂进**子代理自己的 layer**，own-layer 名不在
        # restrictableNames 内，留在名单里 = 每次派发 throw
        # （tools.restrict() names unknown global tool "subagent"）。
        # 嵌套派发改由 plugins/kix-cost.js 的 tools.guard + harness maxDepth
        # 拒绝。条件挂载名由 plugins/kix-cost.js 分层：仅 depth-1 subagent_lite
        # 机械取证放行，其余 tier/goal/workflow 拒绝。deny 名同时从 tools 与 TypeScript
        # 工具镜像消失。run_code 不在名单：它是 Code Mode 保留传输名，
        # restrict 不能命名（WSL2 实弹实锤），作为正当执行通道留在子代理
        # 可见面，其工具镜像一并保留。subagent×2/agent 控制是「编排
        # 留主线程」（kix 地板②）的主线程职责。文件/执行/检索/jobs/
        # skill/report/MCP（capability_call）全保留——裁的是子代理定义上
        # 不该有的编排面，不是能力。
        toolFilter:
          deny: [ exit_plan_mode, subagent_cross, interrupt_agent, send_message, list_agents, ask_user_question, kix_tool_activate, kix_tool_deactivate ]
        persona: |-
          You are kixpower-dev (Nova frontend / Sage backend / Milo design —
          one contract, three hats): the coding member on the CEO team.
          Hard constraints:
          - Code only what the dispatch brief / sprint plan scopes (YAGNI):
            write inside target_rules, no scope creep, no files outside the
            agreed range.
          - Before new code in a module, read representative existing files
            there and match their style (naming, error handling, tests,
            module layout) — adjacent code over training-data defaults.
          - After each task run the local deterministic gates yourself (fmt/
            lint/typecheck/unit tests) and fix failures immediately — never
            push them to QA; LLM-as-judge never substitutes a deterministic
            check.
          - Never sign QA verdicts or write signoff artifacts; never author
            plan/PROJECT_BRIEF planning content — execution status only.
          - Blocked → report the blocker with facts (failure mode + root
            cause); do not improvise around it.
          Report: what changed / gate results / known issues, tables over prose.
        agentOptions:
          maxTokens: 65536

    # Product providers are host-plane singletons. Copy this preset, then
    # remove `disabled` from either ordinary tool row to expose that product
    # only to agents composed from the copy.
    - id: tool-subagent-codex
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: codex
        toolName: subagent_codex
        enableRunInBackground: false
        maxDepth: provider-managed

    - id: tool-subagent-claude-code
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: claude-code
        toolName: subagent_claude_code
        enableRunInBackground: false
        maxDepth: provider-managed

    # 2026-08-15 精简决策：workflow/goal 低频重型 → 默认 disabled。
    # goal 首次使用自动激活（kix_capability_call { tool: create_goal } 挂载
    # 并执行；或 kix_tool_activate { tool: goal } 预激活——2026-08-16 实测
    # 激活 → 下一轮直呼 → deactivate 卸载，闭环可用）。
    # 2026-08-16 启用 workflow（自发使用测试）：workflow 依赖 workflowEngine
    # （本 isolate realm 内提供），动态挂载在 realm 外解析不到 → 激活会
    # 诚实报错；唯一启用路径 = 取消 disabled 重启。2026-08-20 裁决（悬置
    # 解除）：保持启用——真实自发使用在档（kix-task-log-analysis.md：53
    # 会话 4 次 workflow spawn）；EXP3 ⑦ 常驻工具无仪式性滥用，闲置成本
    # ≈1 条 schema；启停摩擦不对称（关闭后需重启宿主才能再启用）。死亡
    # 条款：连续一个月真实会话零使用 → 恢复 disabled（随下次发版重启
    # 落地）。ralph 已移除（极低频 + 可替代，
    # 负债判定）。worker 行必须留在本 realm 内——曾移出导致重启后 UI 损坏
    # （commands/list 空 + 设置 Failed to fetch + 轮询风暴），已回退
    # （2026-08-16）。
    - id: workflow-worker-thread
      name: '@deepseek-ai/dsh-workflow-worker-thread'
      config:
        provider: spawn

    - id: tool-workflow
      name: '@deepseek-ai/dsh-tool-workflow'

# ── remaining model-facing rows ─────────────────────────────────────────────

- id: tool-ask-user
  name: '@deepseek-ai/dsh-tool-ask-user'

- id: tool-todo
  name: '@deepseek-ai/dsh-tool-todo'
  config:
    allowParallelInProgress: true

# The `web` service and its search provider stay in the host composition; only
# the model-facing tool is per-session.
- id: tool-web
  name: '@deepseek-ai/dsh-tool-web'
  config:
    fetch: false
    searchTimeoutMs: 60000

# ── kix 机械门禁（DSH 原生 hook）────────────────────────────────────────────
# 移植自 kixpower blast-radius/block-source-edit 核心门禁，以 tools/pre-execute
# 监听器形态自动拦截（等价 Copilot PreToolUse hook，scope-filtered 只拦本 preset）。
# v8：终端 SQL 命令位判定 + 控制平面只拦写意图（grep/cat/ls 放行）+ GitHub 前缀
# 可配置（githubToolPrefix，默认 mcp__github__）。
# v9：发布/评论/普通 push 等确认类门禁降为软约束，不再提问；硬 deny 仅保留
# 不可逆破坏性操作。单元测试见 plugins/kix-guards.test.js。
# v13（v1.2.15，少即是多）：控制平面 = 安装面，唯此一条定义；v3 的裸
# agent.cordis.yml 兜底分支删除——它误伤源仓库（v11/v13 两轮豁免补丁皆是
# 给它打的），而「源 vs 安装副本」本是「该相同的数份」的领域：源/外仓
# preset 写归 kix-consistency（身份组 + parity hint）管，guards 只管安装面。
# 无豁免、无谓词、无逐路径规则。诚实边界：cwd 在安装目录内的相对路径写
# 不提醒——那本就是用户显式自迭代（v12 已决策放行）。单元测试见 plugins/kix-guards.test.js。
# 相对路径插件由 loader 以 baseUrl 解析（name 以 "." 开头 → new URL(name, baseUrl)）。
- id: kix-guards
  name: ./plugins/kix-guards.js

# ── kix-discipline（P0 纪律机制化，2026-08-16）：需求三检/验证 gate 从
#    prompt 说教改为机制强制。吸收生态插件（dsh-doublecheck 等）的
#    「门禁+工具+持久状态」结构，不取其规则内容（kix 三检 ≠ 六维 grill）。
#    - tools/pre-execute：实现 edit 前查 spec 契约；无 spec + 首次实现编辑 →
#      remind（默认，放行+注入提醒，remindOnce）/ ask（聊天内提问）/
#      block（deny）。测试文件永远放行。
#    - kix_discipline_spec 工具：记录需求三检契约（goal/xy/assumptions/
#      path/acceptance），写工作区 kix-discipline/spec.md（跨会话可查）。
#    - agent/turn-stopping：回合结束有实现 edit 无测试运行 → green 提醒。
#    - /kix-discipline 命令：status/report/on|off。
#    强度默认 remind（限制越少越好；字面明确低风险可逆任务可忽略提醒直接做，
#    与 kix 需求三检"仅信号命中触发"一致）；block 需显式配置。
#    单元测试：node plugins/kix-discipline.test.js（含 mode 编曲留痕）。
- id: kix-discipline
  name: ./plugins/kix-discipline.js

# ── kix-orchestration（P2 补缺，2026-08-16；v2/v3 2026-08-16）：编排交接门禁机制化 ──
# 背景：Copilot 侧 kixpower 的 9 个 hooks 中只有 blast-radius 被 kix-guards 接管，
# validate-handoff 等编排纪律迁移后变"仅 prompt 约束"。本插件做有界融合：
#   - subagent 分派携带 current_sprint 时，校验工作区 docs/.kixpower-current-sprint
#     marker 一致、docs/sprint-N/{plan,progress}.md 存在、progress 无 blocker、
#     切 QA 时任务全部完成（默认 remind；ask/block 需显式配置）。
#   - v2 subagent/end：QA 返回**明确正向**完成声明但 progress.md 未同步 → steer
#     提醒（remindOnce）；v8 负向表述（not done/undone/not passed）不再误报。
#   - v3 producer_closeout 收尾证据链：spec「验收标准」在档 / 任务全完成 /
#     测试文件自 l2_verified_sha 有变更要求重验（替代 Copilot qa-signoff 深度校验
#     中可机械的部分；worktree/SHA 数学/stash 绑定 Copilot 特有流程不移植）。
# 单元测试：node plugins/kix-orchestration.test.js（含 review epoch、Tri-Block Sprint 容错与 plan 契约）。
- id: kix-orchestration
  name: ./plugins/kix-orchestration.js

# ── kix-consistency（P5，2026-08-17；v1.2.15 泛化）：一致性守护写时拦截 ──
# CI 脚本（scripts/check-dsh-consistency.cjs）只在测试期校验，改 preset 文件时
# 不实时拦截 drift。本插件把「唯一事实源」从自觉变机械：写身份组成员时跑
# **相关子检查**，与 CI 共用 consistency-lib.cjs 单一事实源（防「CI 一套、
# 运行时一套」双源漂移）。边界自感知：preset 根 = 同时含 agent.cordis.yml +
# preset.yml 的目录（深度 ≤2 扫描）；任意仓库 ≥2 根才引导——通用层只引导
# 各根同名 plugins 字节一致（该相同的数份必须相同，N ≥ 2）；shell 写入不做
# 机械提取（命令文本启发式是负债——软启发 + CI 兜底）；其余根内路径
# （skills/agents/prompts 等翻译关系）只发 parity hint——不断言失败，把
# 「其它根对应份是否同步」交给模型判断（没说到的形态靠提醒感知）。
# 边界即 preset 根：非 preset 根路径天然出组（自感知推论，无逐路径豁免）。
# kix 全量契约（persona 预算 /
# memories 计数 / README 表述 / 版本对 / vision-bridge / 单文件语法）由仓库
# 自带 scripts 入口自声明触发，外仓不硬套。强度默认 remind（只做启发引导）；
# ask/block 需显式配置。remindOnce：每会话每类别一次。
# 单元测试：node plugins/kix-consistency.test.js。
- id: kix-consistency
  name: ./plugins/kix-consistency.js

# ── kix-cost（v5.8 成本纪律）：机械档自动选型 + 子代理思考强度分层 ─────────
# agent/request waterfall 上：
#   A. 轻量子代理（预算帽 ≤8K，subagent_lite）首次请求探测首选路由可用性
#      （llm.listProviders + resolveModelInfo）；不可用 → 回退环境默认路由
#      （agentDefaultModel.currentSelection()）——preset 分发到不同环境不挂。
#   B. 无显式 effort 的 deepseek 子代理按预算帽注入：≥98K（subagent_thinker）
#      → max，其余 → high（默认档；64K 预算帽防跑飞——适配器默认上限 256K）。
# 主会话/GLM 子代理/显式 effort 均不干预。纯监听器、不发布服务，无需 realm。
# 见 plugins/kix-cost.js 头部注释；单元测试：node plugins/kix-cost.test.js。
- id: kix-cost
  name: ./plugins/kix-cost.js
  config:
    # effort 归一化的目标厂商族（provider id 前缀；2026-09-10 起插件不再逐字
    # 比较某个 provider id）。改部署换入口名时改这里，或整块删掉 = 默认 deepseek。
    effortVendor: deepseek

# ── kix-route（子代理路由层）：哨兵模型名 → 运行时可用路由自动解析 ─────────
# 工具行不再钉死 (provider, model)：cross/vision/thinker 三行只钉
# `kix-route:<tier>` 哨兵，本插件在子代理首次请求的 agent/request waterfall
# 里按 llm 实时目录解析——cross = 父厂商取反、vision = 首个声明 image 输入
# 的模型、thinker = deepseek 系首选。边界语义（v5.9.1）：cross 在单厂商部署、
# vision 在无视觉模型部署启动即报错（错误信息附改用建议，带回父模型——
# 假独立性比失败更糟）；thinker 无 deepseek 时降级环境默认（角色仍成立）+
# 告警；插件缺失时哨兵直达适配器响亮失败（不静默同厂商降级）。与 kix-cost
# 顺序无关协作（effort 规则同源复用）。见 plugins/kix-route.js 头部注释；
# v8：cross 偏好表候选先过滤未注册 provider，目录探测失败不再放大为 cross 失败。
# v13：cross provider 可用性失败时同一 child 最多换 2 家健康异厂商；耗尽才零证据终止。
# 单元测试：node plugins/kix-route.test.js。
- id: kix-route
  name: ./plugins/kix-route.js
  config:
    # Generic QUOTA TTL; HTTP 402 / Insufficient Balance stays process-wide open until restart/reload.
    providerCircuitTtlMs: 300000
    crossProviderFailovers: 2
    fallbackProviderOrder: [ su2api, zai-coding-cn, deepseek-official ]
    # ── 路由偏好 = 本部署事实（2026-09-10 从插件默认表搬来）────────────────
    # 按「可获取的方式」原则：插件不再内置任何模型/厂商 id；下面每一条都是
    # 本机 settings.yaml 里已注册的 provider/model。换部署改这里，或整块删掉
    # （删掉 = 目录序，任何部署都能跑；只是失去本机的成本/厂商偏好）。
    modelPreference:
      zai-coding-cn: [ glm-5.3, glm-5.3-flash ]   # 本机 zai catalog 实际只有这两个
    crossProviderOrder:
      zhipu: [ deepseek-official ]      # 父=zai 系 → 跨厂商取反到 deepseek
      deepseek: [ zai-coding-cn ]
      grok: [ zai-coding-cn ]
      xai: [ zai-coding-cn ]
    genericCrossOrder: [ su2api, zai-coding-cn, deepseek-official ]
    # 档位 provider 提示（可选；缺省 = 目录序。能力判定仍走目录：
    # vision 要求模型显式声明 image 输入，thinker 要求 deepseek 同族前缀）
    visionProviderHint: [ zai-vision ]
    thinkerProviderHint: [ deepseek-official ]

# ── presentation（PTC/Code Mode 融入）───────────────────────────────────────
# `both` = native 工具 schema 与 Code Mode SDK（run_code + tools.* 绑定）并存，
# 不声明 code-only 规则 → native 直呼照常可执行，模型自主选择形态：
#   - 单步/验证/观察动作 → native 直呼（证据可回放、门禁逐条可见）
#   - 机械多步序列/批处理/并发探测 → run_code 程序（一次往返，Promise.all 并发，
#     try/catch 容错；SDK 子分派走完整 pre-execute→guards→execute→post-execute 管线，
#     门禁依然拦截；仅 print/return 回流，中间值不可重建）
# 行等待宿主 `codeRuntime`（web-app bundle 已组装 dsh-code-runtime-worker-thread）；
# 未组装运行时的部署会在挂载时指名此 id 拒绝。
- id: tool-presentation
  name: '@deepseek-ai/dsh-agent-tool-presentation'
  config:
    mode: both

# ── kix 流程命令（P1-8：DSH 原生命令平面，2026-08 落地）────────────────
# 注册 /kixpower-new /kixpower-import /kixpower-continue /kixpower-review
# /kixpower 五个原生命令：UI 命令候选 + 零 token 触发 + handler 读
# prompts/*.prompt.md 注入 user 消息（与 /plan 命令同语义）。
- id: kix-commands
  name: ./plugins/kix-commands.js

# ── kix-focus（极简 + 渐进披露 + PTC 协同，2026-08-16，三层递进）─────────
# 背景：kix 会话每轮向模型发送全部工具 schema（约百 KB），极简模式证明
# 裁剪工具面可显著降本增效（dsh-mcp-lens 实测输入 token ↓89%、成本 ↓89%）。
# 三层递进（设计见 classic 档 PLUGINIZATION-ROADMAP.md §8）：
#   Phase 1 — tools.restrict 裁剪到常驻核心集（三通道执行/观察/
#     交互 + 发现入口），MCP（GitHub/Playwright/Context7/Semgrep）与
#     cordis_* 等宿主全局工具按需；tools/change 事件重试。
#   Phase 2 — kix_capability_search（按需目录查询，返回分组元数据不含全
#     schema）+ kix_capability_call（代理执行，经 ctx.tools.execute 走完整
#     pre-execute→guards→execute→post-execute 管线，门禁依然拦截）。
#     scope 工具 restrict 裁剪不到：workflow/job_*/子代理控制及 reviewer/dev/qa
#     常驻；goal 与 lite/thinker/vision/fork 按需激活；ralph 已移除。
#   Phase 3 — 保持 tool-presentation mode:both（native 直呼验证 + run_code
#     机械多步），kix 红线「验证/观察用 native 直呼（证据可回放）」不变；
#     capability_call 亦可被 run_code SDK 子分派调用（子分派过门禁）。
# 关闭裁剪（仅保留 search/call）：config: { enableRestrict: false }。
# 单元测试：node plugins/kix-focus.test.js（含常驻成员/枚举回归防线 +
#   symlink 部署跨平台解析：候选根链 argv[1]→realpath→插件文件，2026-08-17
#   WSL2 E2E 发现修复；jobs 常驻化 + 首次使用自动激活；v1.2.13 lite 平台
#   条件化 shell + 编曲成员 current_sprint 机械注入用例）。
- id: kix-focus
  name: ./plugins/kix-focus.js

# ── kix-stalled（L3 档 A：stalled 机械检测，2026-08-15；2026-08-20 用户全开裁决启用）──
# 只读、无状态、无常驻：/kixst-check <项目根> 命令（零 token）+ kix_stalled_check
# 模型工具。candidate keep（2026-09-08 结算）：kix-bundle 主会话调用 5 次，
# 无外部 sprint 资产，不晋级；真实 sprint 仓再评。08-20 全开不再当出生证明。
- id: kix-stalled
  name: ./plugins/kix-stalled.js

# ── kix-signal（spec-draft 单信号，2026-08-18 三信号生 / 08-19 两轮收敛）──
# S3 spec-draft：首次源编辑无需求契约 → 注入五字段草稿模板（写作题变改错题）。
# 设计依据：提醒通道合规 2/2 vs 模型自驱 13%（349 会话对照）；与 kix-discipline
# 的 red-remind 同触发各提醒一次（discipline 提醒 + 此处脚手架），语义互补。
# S1 poll-guard / S2 orchestration-trigger 已退役（与宿主 repeat-tool-reminder /
# kix-budget streak+gate 重叠，三层证据判退——完整死亡证明见插件头注释）。
# 全 advisory 零 deny。回滚：注释下面整块并重启宿主。
- id: kix-signal
  name: ./plugins/kix-signal.js
  config:
    specDraft: true

# ── kixparadigm (incentive) additions (2026-08-19, EXP1-3 findings as mechanisms) ──
# probe: neutral fresh-process executor with free duration/peak-memory telemetry
# (EXP3: correct adoption, zero cost premium, zero ritual use of placebos).
- id: kix-probe
  name: ./plugins/kix-probe.js

# settle v5: delivery-time steer (advisory, each path once per session).
#   ① any agent: source/test edits lack current-generation terminal evidence.
#   ② root authority only: standalone high-confidence verdict with no fresh observer.
#   ③ stable 1/16 sample: terminal-evidence 1–2-file root edits without fresh observer;
#      counterexamples reclassify risk, zero findings never lower verification.
- id: kix-settle
  name: ./plugins/kix-settle.js

# mem: pull-only crisis-indexed experience library, zero resident context
# (user-verified: agents rummage past experience when stuck).
- id: kix-mem
  name: ./plugins/kix-mem.js

# kix-browser（常驻挂载=用户裁决 2026-08-20，与默认版同步——null 变体同
# 插件面；死亡条款与完整背景见默认版注释：一个月真实使用 <2 次回退渐进披露）
- id: kix-browser
  name: ./plugins/kix-browser.js

# kix-webhook（外部事件 → kix 会话，2026-09-09；与默认版同步——null 变体同
# 插件面，parity 契约见 plugins/kix4.test.js）。常驻加载但 config.enabled: false
# → apply 直接返回，无注入/监听/模型可见面，对消融面无行为影响。
# 完整背景与启用步骤见默认版注释与 dsh/preset/patches/kix-webhook.reference.yml。
- id: kix-webhook
  name: ./plugins/kix-webhook.js
  config:
    enabled: false
    workspacePath: /absolute/path/to/workspace
    agentPreset: kixparadigm
    permissionPreset: danger-full-access
    maxSessions: 1
